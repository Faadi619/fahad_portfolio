# ──────────────────────────────────────────────────────────────
# Source material — NEVER commit. This repo is public.
#
# "Fahad Documents/" holds identity and financial records: CNIC,
# domicile, bank statements, tax certificates, a signature image,
# ngrok recovery codes, and a family member's documents. None of
# it belongs on GitHub. The site needs none of it at runtime —
# build-images.sh reads these locally and writes into site/.
# ──────────────────────────────────────────────────────────────
Fahad Documents/
AppSpot Banners/

# Portrait source. build-images.sh copies it to assets/img/fahad.png,
# which is the version the site actually serves.
Fahad Image.png

# The brief, kept local.
Initial_Instructions.md

# ── OS / tooling ──────────────────────────────────────────────
.DS_Store
**/.DS_Store
node_modules/
*.log

# ── Scratch ───────────────────────────────────────────────────
.tmp/
*.tmp
